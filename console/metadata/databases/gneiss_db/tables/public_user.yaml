table:
  name: user
  schema: public
select_permissions:
  - role: frontend_user
    permission:
      columns:
        - email
        - hashed_refresh_token
        - id
        - password
        - username
      filter:
        id:
          _eq: X-Hasura-User-Id
